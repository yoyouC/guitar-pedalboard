# CONTEXT

本文件的用途:固定项目的领域词汇(ubiquitous language)。讨论架构、写 issue/ADR、命名模块时,优先使用这里的词。代码标识符用英文,行文用中文。

## 信号链(从吉他到耳朵)

- **Rig** —— 一份完整可复现的配置:效果链 + 箱头前均衡 + 箱头 + 箱体 + 全局参数(inputGain、masterVolume、globalBypass)。它是快照、预设与分享表示的 canonical 来源。
- **效果链(Chain)** —— 有序的单块列表,每项(`ChainItem`)在单次装载的运行态内有稳定身份 `uid`、效果 `effectId`、开关 `enabled`、参数 `values`、前后置位 `post`;模型型单块另有外部 `modelRef/modelId`，不把动态模型身份塞进 `effectId`。
- **单块(Pedal)** —— 效果链上的一级效果器(过载、延迟、混响、哇音……)。注册于 `src/audio/effects/`。
- **箱头前均衡(Pre-Amp EQ)** —— 效果链前置单块之后、箱头之前的固定十段图示均衡级；它是 Rig 中唯一的专用级，不是可拖动 Pedal。自己的 bypass 只令频响与 Level 回到单位增益，仍记住并允许编辑参数；与 Amp 自带的 Bass/Mid/Treble 独立。
- **箱头(Amp)** —— 效果链之后的放大器级,有自己的开关与参数;同一箱头可能有多个**模型(model)**(如 NAM 扫档包的档位)。
- **箱体(Cab)** —— 箱头之后的箱体模拟级。
- **FX Loop / 后置位(post)** —— 单块可放在箱头前(默认)或箱头后/箱体前(`post: true`)。
- **NAM** —— Neural Amp Modeler,经 WASM 运行的神经网络箱头/单块模型;有**扫档包(sweep pack)**(一个模型按增益分档)。
- **WDF** —— Wave Digital Filter,白盒电路建模的另一条 DSP 路线(与 NAM 并列)。

## 状态与持久化

- **rigStore** —— Rig 状态的单一事实源(模块级 pub-sub store,见 ADR-0002)。引擎是它的被动投影;快照/预设/URL 三条恢复路径都经 `applyRig` 收口。
- **快照(Snapshot)** —— 板载 A/B/C/D 四个槽位的 Rig 即时存取(= Rig − 全局参数),含 ampCategoryId+ampModelKey(走型号机制);踩钉长按清空。槽位有 **dirty**(已修改)状态。序列化上它是 canonical Rig 表示的派生(ADR-0006);旧形状(ampId-only)宽容解析,回退"保持当前型号"行为。
- **预设(Preset)** —— 用户在私人本地工作区中命名保存的 Rig；它可以导入导出，但不会因登录而自动同步或自动进入音色广场。
- **分享(Share)** —— 不依赖平台账号或服务端记录的 Rig URL；与“发布到音色广场”是不同概念。

## 音色广场

- **广场预设(Published Preset)** —— 创作者从完整 Rig 显式发布的广场作品，拥有稳定作品身份和一条只追加的声音修订线；标题、介绍、标签等可变展示信息属于作品，不属于 Rig 修订。
- **预设修订(Preset Revision)** —— 广场预设在某次发布时冻结的 canonical Rig；修改声音产生新修订，既有修订不被覆盖，修订永久链接始终指向同一声音。
- **当前修订(Current Revision)** —— 广场预设普通详情页默认呈现的修订；从旧修订回退时，以旧声音创建新修订并将其设为当前，而不改写历史。
- **不兼容修订(Incompatible Revision)** —— 因客户端不支持其 Rig schema 或目录中的器材而无法忠实应用的预设修订；作品和原始修订继续存在，系统不忽略字段或静默替换器材来伪装成功。
- **预设合集(Preset Collection)** —— 创作者维护的可变策展作品，由一组有序的合集条目构成；它只组织预设，不承载 NAM、Cab IR 或其他二进制资源。避免称作“预设包”，以免与 NAM 扫档包或资源下载包混淆。
- **合集条目(Collection Item)** —— 预设合集中的一个有序位置，固定引用某个具体预设修订并展示原作者；预设发布新修订不会令合集条目自动变声。
- **不可用条目(Unavailable Item)** —— 被引用的广场预设撤回后留在合集中的占位；系统不自动删除或替换它，由合集作者处理。
- **发布(Publish)** —— 将私人本地预设显式送入音色广场的动作；不是本地保存、URL 分享或自动云同步。
- **来源(Provenance)** —— 本地 Rig 或预设从某个广场预设修订加载而来的归属关系；本地保存和继续编辑不清除来源，再次发布时形成改编。只有从空白或出厂 Rig 明确开始才没有来源。
- **应用恢复点(Apply Restore Point)** —— 应用广场预设前在当前浏览器会话中暂存的原 Rig，用于撤销本次应用；它不是本地预设、快照或云端草稿。
- **访客(Visitor)** —— 未登录音色广场的人，可以发现并应用广场预设，但不能发布、管理内容或点赞。
- **成员(Member)** —— 拥有本站稳定身份的已登录参与者，由内部 id 标识，并以唯一 handle、显示名及可选头像和简介公开呈现；TONE3000 账号只是可选外部连接，不是成员身份。
- **账户注销恢复期(Account Recovery Window)** —— 成员申请注销后、永久清除前的 30 天窗口；期间账户停止社区写入，作品与合集撤回，成员再次验证后可取消注销。
- **成员占位(Member Tombstone)** —— 恢复期结束后为作品来源、合集条目与治理记录保留的不可复用身份占位；它不包含公开资料、明文历史 handle 或登录身份，也不能恢复已清除正文。历史 handle 只保留 SHA-256 预留摘要以阻止冒充复用。
- **改编(Remix)** —— 应用他人的某个预设修订、修改后再发布形成的新作品；新作品归新创作者所有，并永久保留来源作品与来源修订的归属关系。
- **点赞(Like)** —— 成员对他人的一个广场预设或预设合集表达的可撤销认可；同一成员对同一对象至多有一个有效点赞，两类对象分别计数。点赞也构成成员私有的“我的点赞”列表，不等同于公开社交关系。
- **标签(Tag)** —— 由平台维护、成员选择的受控内容分类，拥有稳定身份及中英文名称和搜索别名；每个广场预设或预设合集选择 1–5 个标签。Pedal、Amp、Cab 和资源依赖是从 Rig 派生的筛选属性，不是标签。
- **统一发现(Unified Discovery)** —— 音色广场按预设、合集、创作者分栏的公开搜索入口；三栏各自使用绑定查询和类型的稳定游标。预设与合集只发现 Public 内容，创作者按 handle 和显示名检索，任何合集结果都不投影条目正文。
- **停用标签(Deprecated Tag)** —— 不再供新内容选择、但仍以稳定身份保留的标签；它可以合并到另一个标签，既有内容与搜索别名随之迁移。
- **最受欢迎(Popular)** —— 按有效总点赞数排序的公开内容榜单；广场预设与预设合集分别排名。
- **趋势(Trending)** —— 主要依据近期新增有效点赞并随时间衰减的公开内容榜单；具体公式是可调整策略，不以浏览或应用次数为输入。
- **公开(Public)** —— 可进入音色广场列表与搜索的发布内容。
- **不列出(Unlisted)** —— 仅通过直接链接访问、不进入音色广场列表和搜索的发布内容。
- **公开页面(Public Page)** —— 以作品、合集或成员不可变 id 作为 canonical 身份的可索引页面；标题、slug 或 handle 只负责展示或旧链接跳转。Unlisted 页面保留稳定直接 URL，但输出 `noindex,nofollow`。
- **已撤回(Withdrawn)** —— 不再公开发现或展示正文、但为来源关系和治理审计保留身份的发布内容；撤回原作不级联删除其改编作品。
- **已隐藏(Hidden)** —— 因管理员治理或侵权流程而停止公开展示的内容；与创作者主动撤回不同，只有管理员可以恢复。
- **资源依赖(Resource Dependency)** —— 广场预设复现声音所需的非 Rig 资源。内置资源可直接复现，TONE3000 引用属于需明确提示的外部依赖，本机 NAM 与自定义 Cab IR 不属于可发布资源。
- **外部依赖不可用(External Dependency Unavailable)** —— 广场预设引用的 TONE3000 资源暂时或永久无法取得；作品与 Rig 结构继续存在并明确降级，不被自动撤回或替换模型。
- **举报(Report)** —— 成员针对自己可访问的 Public 或 Unlisted 内容提交的治理请求；另有无需成员身份的正式侵权通知渠道。管理员可以据此隐藏内容或封禁成员，且治理动作保留审计记录。
- **申诉(Appeal)** —— 被治理内容的作者针对一次处理结果提交的复核请求；作者可见处理原因，每次处理允许一次申诉。
- **成员封禁(Member Ban)** —— 只停止成员继续执行社区写操作，不删除成员身份、作品归属或历史事实；被封禁成员的点赞事实继续私有保留，但不计入公开聚合与榜单，解封后可由投影重建恢复有效性。

## 音频基建

- **AudioEngine** —— 音频图的所有者:AudioContext、主链路、worklet 预载、输入源、录音、Looper 宿主、图谱编译(rebuildGraph)。
- **Worklet** —— AudioWorklet 处理器;经 `createWorkletLoader` 按 AudioContext 幂等注册(ADR-0001)。
- **箱体 IR 身份(Cab IR Ref)** —— canonical Rig 只保存可序列化的 `{kind:'builtin', id}` 或 `{kind:'custom', hash}`。WAV Blob、AudioBuffer、加载状态、离线资产校准值与缺失回退都不属于 Rig；Custom 校准是由 WAV 内容确定性派生的本机 Library 元数据。
- **图谱编译(rebuildGraph)** —— 由 Rig 快照重建/复用音频节点图的过程;实例复用语义:uid+def+key 复用单块,def+key 复用箱头与稳定 Cab Runtime，固定 Pre-Amp EQ Runtime 不随参数或 bypass 重建。内部分两层(ADR-0005):**plan**(纯函数决策:创建/复用/销毁清单,无 WebAudio 依赖)与 **execute**(薄执行:节点创建与连线,NAM 的 fetch 等副作用隔离在此层)。
- **DSP 核(`*.dsp.js`)** —— WDF 算法的唯一 canonical 源(纯 JS + JSDoc,与 worklet 同目录)。两个消费对象:worklet 侧经 `?raw` 取源码字符串拼装 Blob,eval/测试侧正常 import 执行。验证的代码 = 发声的代码(ADR-0003)。
- **音频档位(Audio Profile)** —— 当前设备的运行偏好：实时演奏、平衡或稳定播放。它只影响浏览器音频请求，不属于 Rig，也不改变 DSP 音质。
- **输出估算(Output Estimate)** —— 浏览器报告的 `baseLatency + outputLatency` 输出侧估算；不包含输入捕获、Rig 链路或完整物理设备往返。
- **链路时延(Rig Latency)** —— 当前活动 Pedal、Amp、Cab 在直接监听路径中声明并合成的 sample/ms 计算值；处理时延与音乐性的设计时延分开记录。
- **往返时延(Round-trip Latency)** —— 仅指低电平电气回环校准得到的输入到输出实测值；设备或音频环境变化即失效。
- **音频中断(Underrun)** —— 仅指浏览器 `playbackStats` 等音频能力明确报告的中断。主线程长任务、掉帧与压力测试只是独立代理指标。
- **DSP 质量(DSP Quality)** —— 独立于音频档位的音色/资源选择；只有具有真实能力且完成性能与音频验证的模块才可开放。

## 演奏辅助

- **Looper** —— 单轨循环录音(初录/叠录/撤销/清空),挂在全 Rig 输出之后。
- **MIDI 映射(MIDI mapping)** —— 出厂固定的控制器→动作映射(K25、motion_midi 总线)。
- **MIDI Learn** —— 用户自定义的 MIDI 绑定:进入 Learn 模式、点击控件武装(armed)目标、扭/踩控制器完成绑定。
- **RigAction(动作词汇表)** —— 一次"要执行什么"的意图(切单块、召回快照、设箱头参数……),携带映射后的语义值。所有触发源(MIDI 默认映射、MIDI Learn、键盘)统一翻译成 RigAction,经同一 dispatch 执行。与 **MidiTarget**(Learn 绑定的持久化**地址**,回答"绑的是哪个控件")是两个概念:地址 + 原始值 → 翻译层 → RigAction。
- **表情踏板 / 摇杆(Treadle)** —— CC 连续控制源,驱动哇音 position、Whammy 移调等连续参数。
- **TONE3000** —— 外部 NAM 模型分享平台(tone3000.com),经官方 API v1(OAuth 2.0 + PKCE)接入;托管 Select 可为 Amp 或 Pedal 限定 gear/architecture，模型按用户身份在请求时下载(ADR-0007、ADR-0008)。
- **tone_id / model_id** —— `tone_id` 是 tone pack 的稳定数字 id，编码为外部引用 `tone3000:{toneId}`；`model_id` 是其中一次托管选择的精确 NAM 变体。Preset、Snapshot、Share 保存引用和可用的精确变体，恢复失败只改变逐目标运行态，不丢用户意图。
