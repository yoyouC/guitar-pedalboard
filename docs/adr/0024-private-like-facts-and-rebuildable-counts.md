# 私有点赞事实与分类型可重建聚合

预设与合集分别保存以 `(member_id, target_id)` 唯一的有效点赞事实，成员写入只提交目标身份和期望状态，不能提交计数；个人点赞状态与“我的点赞”直接读取事实表且只对本人开放。两类公开 Popular 榜分别读取可重建计数投影，按有效总点赞数和不可变目标 id 稳定定序；分页游标固定一个排名版本，后续点赞变化不会让同一次遍历漏项或重复。投影及其版本历史在幂等写入事务中更新，并可从事实表完整重建。榜单和公开作品不暴露点赞成员或个人轨迹，也不引入浏览、应用或演奏行为信号。
