# 标签合并保留源身份并在单事务内迁移关系

受控标签合并不删除或复用源 id，而把源标为 Merged Tag 并永久指向一个活动目标。Preset、Preset Collection、别名和旧筛选 id 在同一事务内迁移或转发到目标，审计记录也在该事务提交；目标已包含同一内容关系时使用集合语义去重，重复执行相同合并成为无副作用成功。这样比保留两套内容关系更容易维持新选择的一致性，同时避免旧链接、保存的筛选条件与历史解释失效。
