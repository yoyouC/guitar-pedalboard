# 音色广场与本地工作区、资源库保持显式边界

音色广场只接收成员主动发布的完整 Rig，不自动同步本地预设，也不把既有 URL Share 变成服务端记录。首期只允许可直接复现的内置资源和带依赖提示的 TONE3000 引用；本机 NAM 与自定义 Cab IR 不上传、不可发布。这个边界牺牲了私人预设跨设备同步和任意资源完全复现，以延续本机资产的隐私承诺，并把账号、版权、存储与恶意文件处理留在独立的未来设计中。
